server:
  port: 8081

jwt:
  secret: "secret123"

spring:
  # Templates reloading during development
  thymeleaf:
    prefix: file:src/main/resources/templates/
    cache: false

  # Static resources reloading during development
  resources:
    static-locations: file:src/main/resources/static/
    cache-period: 0

  datasource:
    url: jdbc:mysql://localhost:3306/pro_fit_app
    username: root
    password: password
    name: pro_fit_app