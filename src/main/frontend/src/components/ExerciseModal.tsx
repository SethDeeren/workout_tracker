import React from 'react';

const ExerciseModal: React.FC = (props) => {

    const handleSubmit = () => {
        // props.addExercise(this)
        // props.showExerciseModalHandler to hide this modal
    }
    return (
        // need handle backdrop and modal overlay
        <div>
            
        </div>
    );
}

export default ExerciseModal;